# Runtime dependencies (see pyproject.toml for the package metadata)
numpy>=1.26
scipy>=1.11
optuna>=4.0

# Tests
pytest>=8

# Optional GPU backend (CUDA 12); pick the wheel that matches your CUDA version
# cupy-cuda12x
