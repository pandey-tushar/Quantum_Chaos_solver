"""qrc_bench: quantum reservoir computing vs tuned classical baselines.

Every stage (task, encoder, reservoir, readout, baseline) is looked up by name
in ``qrc_bench.registry`` so it can be swapped from the CLI.
"""
__version__ = "3.0.0.dev0"
